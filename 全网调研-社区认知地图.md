# 全网调研 · 社区认知地图

> 一个 harness，可以理解为跑在终端/网页里、驱动 AI agent 干活的"外壳程序"。本文把网上关于 DeepSeek Harness（下称 dsh）的各种说法汇到一处——公司理念、它的技术血缘、发布后的反响——并给每条结论**标上证据等级、点出彼此的冲突**。规则很简单：外部说法默认按"有人这么讲"（`[claimed]`）对待，只有经过一手核实的才升级。这样读者一眼就能分清哪些是事实、哪些还只是传言。

## A. 文章/来源索引（谁说了什么）

先把牌摊开：下表列出所有信息源，以及每条内容的可信度。三个标记的含义是——`[verified]` 表示已亲自核实（读了原文或跑了本地验证）；`[inferred]` 表示证据很强但缺一句逐字实锤，属于高置信度推断；`[claimed]` 表示只是某方的说法，尚未独立验证。

| 来源 | 类型 | 核心内容 | 证据等级 |
|---|---|---|---|
| deepseek.com/harness（官网） | 官方 | "模型是 agent 的灵魂"；可替换插件；append-only 可追溯会话日志；预设模式 | `[claimed]`（官方口径） |
| @deepseek_ai 发布推文（2026-08-13） | 官方 | 开发者预览发布 | `[claimed]` |
| HN "DeepSeek Harness developer preview"（item 49285244，569 分/247 评） | 社区 | 主讨论场：竞品对比、Cordis 评价、体积/TS/供应链质疑 | `[claimed]` |
| cordiverse/cordis（README） | 一手 | "A Meta-Framework of Spatiotemporal Composability"；Context/service/plugin/effect | `[verified]`（我已 WebFetch 核实） |
| cordiverse/paper（preprint 2026-08-13） | 一手 | 时间/空间可组合性形式化，实现于 Cordis | `[verified]`（摘要已核实） |
| koishijs/koishi | 一手 | Shigma 的跨平台聊天机器人框架，插件市场/HMR | `[verified]` |
| x-cmd / xlings 打包页 | 社区 | `x install dsh`；称首日 ~13k stars | `[claimed]`（star 数未精确核实） |
| 暗涌/ChinaTalk 梁文锋访谈、开源周 open-infra-index | 一手/官方 | DeepSeek 公司理念（见 C 节） | `[verified]`（原文可查） |

## B. Cordis / 时空可组合性 / Koishi 血缘（本研究最强、最被低估的发现）

这一节是全文最值得慢读的部分。它回答一个容易被忽略的问题：dsh 底下那套插件内核，到底从哪来？

- **Cordis 不是 DeepSeek 自创**：它本是一个已有的 TypeScript 元框架（出自 cordiverse 组织，作者 @shigma）。所谓元框架，就是"用来搭框架的框架"——它自己不做具体业务，只提供组织插件、注入依赖的底座。dsh 采用它的方式很彻底：不是当普通依赖装，而是把源码整个搬进仓库（业界叫 **vendored 源码**），锁定在 `4.0.0-rc.7` 版本，并改名为 `@deepseek-ai/cordis`；同时维护 cordis/cosmokit/schemastery 三个 fork，还把补丁回合并给上游。一句话概括：**一个顶级 AI 实验室，把它的 agent harness 建在了一个社区聊天机器人框架的依赖注入（DI）/插件内核之上。** `[verified]`（vendor/README.md + Cordis 仓库）
- **血缘**：@shigma 同时也是知名聊天机器人框架 **Koishi** 的作者；Cordis 正是从 Koishi 里抽象出来的通用内核——同一个作者，加上插件市场、HMR（热模块替换，即改代码不重启就生效）、DI 这套共同的架构基因。"Koishi 建立在 Cordis 上"这一点置信度很高，只是本次没能拿到逐字的一手原话 → `[inferred]`。
- **Shigma 本人就是 dsh 仓库的直接提交者** `[verified]`：README 里那条 paper 链接，正是 Shigma 亲手提交的（commit `0ae8f27b93`，`docs: add link to preview paper`，作者 `shigma10826@gmail.com`）。这条把"Cordis↔dsh↔Shigma"的三角关系从推断变成了实锤——Cordis 的作者是直接下场参与 dsh 开发的，而不只是被人引用。
- **关于那篇"论文"——它并不是 DeepSeek 的论文** `[verified]`：dsh 引用的唯一一篇论文是 Cordis 的《A Programming Paradigm for Spatiotemporal Composability》（署名 cordiverse 组织，页面上没有 DeepSeek）。它讲的是**底层的插件范式**，与 DeepSeek 的模型或 harness 产品无关。值得一提的是，同一个提交还**删掉了仓库里原先内置的 `docs/cordis-paper.pdf`**（1.24MB → 0），改成外链预印本。截至研究日，**没有发现任何由 DeepSeek 自己发表、关于这个 harness 的学术论文**。
- **论文《A Programming Paradigm for Spatiotemporal Composability》**（摘要已直接核实 `[verified]`）讲了什么：
  - **时间可组合性**：组件卸载时能"完全回滚它造成的副作用"——即 Revertible Effects（可撤销的副作用，每个上下文变换都自带一个可追踪的逆操作）。打个比方：像给每次改动都配了一颗后悔药，拔掉插件就能干净地退回原状。
  - **空间可组合性**：用响应式的方式声明组件之间的依赖——即 Reactive Coeffects（上下文一旦匹配某个 spec，就自动通知相关组件）。
  - 它把 effect（副作用）和 coeffect（对上下文的依赖）统一进同一个 context type + 一套组件演算，最终落地为 Cordis 的 effect/coeffect 引擎，外加一个带 HMR 的声明式 loader。
  - 这套范式**直接对应** dsh 的那条铁律"Registrations are effects"（任何注册都是副作用，`ctx.effect()`/`ctx.on()` 都会返回一个 disposer——即"用完负责清理自己的回收句柄"）。→ 详见 Ch03。

## C. DeepSeek 公司理念（背景基线，用于 Ch20 映射）

来自暗涌/ChinaTalk 梁文锋访谈、开源周与技术报告（原文可查，`[verified]`；具体措辞见来源）：

- **量化基因、没有外部 VC**：2023-07 从量化基金幻方量化里独立出来，梁文锋控股。用他的话说，"钱不是问题，禁运的先进芯片才是问题"。
- **原创 vs 模仿（核心命题）**：他认为中美真正的差距"不是一两年，而是原创和模仿的差距"；目标是让中国"成为贡献者，而不是搭便车的人"。
- **护城河是团队，不是代码**："开源、发论文没有成本"，真正的护城河是"能持续创新的组织与文化"。因此开源在他们眼里是一种声誉/人才/标准层面的战略，而非做慈善。
- **扁平、好奇心驱动**：没有 KPI、没有硬性岗位划分；招牌技术 MLA 最初就源于一位年轻研究员的个人兴趣。招人看重的是"热爱 + 扎实的基础"。
- **"少即是多"的工程哲学**：MLA（多头潜在注意力，一种省显存的注意力机制）、MoE（专家混合，每次只激活一部分参数以省算力）、FP8（用 8 位浮点做训练，进一步压低成本）、计算-通信重叠——都是在"用更少的算力做更多的事"。开源周放出来的，也都是"在生产里实测过、不是花架子（vaporware）"的底层基建。

## D. 争议观点矩阵（同一主题的不同解读）

同一件事，社区里往往有两派看法。下表把几个主要争议并排列出——左边一派、右边一派，最后一列是结合源码后本研究更偏向哪边。

| 主题 | 观点 A | 观点 B | 本研究倾向 |
|---|---|---|---|
| 一方 harness 是否更优 | 官方 harness 配自家模型，协同更强 | 一方 harness 未必就赢第三方（HN rco8786） | 从源码看，它的价值在于"可替换性/生态"，而非把你绑死在自家模型上 `[inferred]` |
| Cordis 是否"新" | "genuinely new"，并非 Pi 的衍生品（HN tosh） | 说白了就是"热重载 + 动态启停的插件系统"（HN lxdlam） | 论文里的形式化 + 可逆 effect，才是它真正区别于普通插件系统之处 `[inferred]` |
| 跨插件 DI 的实用性 | 强制 cleanup 的模式很好（HN badlogic） | 跨插件 DI 实际能带来多少收益，存疑 | 需在 Ch11 用源码来验证 |
| TS/体积 | — | 47MB 下载 / 1.5GB build、选 TS 是否合适、npm 供应链风险等质疑 | 记为已知局限，留待 Ch19 讨论 `[claimed]` |
| "agent 写的代码库" | **官方自述**：`.agents/notes/.../2026-06-11-quality-gates.md:11` 里白纸黑字写着 "This codebase is developed primarily by coding agents"（中文版同）+ 结构性证据（codex/agent 分支、self-modification） | HN 上没有高赞的明确讨论帖 | 因有一手自述，可从"推断"**升级为 `[verified]`**；Ch20 展开 |

## E. 高频启示（社区共识）

有几点是社区里反复被提到、基本形成共识的：

1. dsh 属于 terminal/web agent harness 这一类，和 Claude Code / Codex CLI / Pi / Cascade 是同类产品。
2. 它对工具调用采用了严格的 JSON schema（即用一套明确的格式约束模型该怎么调工具），这点收获好评（HN 上 JonChesterfield 认为比 Codex 做得更好）。
3. Cordis 那篇论文，被普遍认为是整个项目里最有"研究味"的部分。
4. 在中文这边，社区走的是企业微信群 + 微信公众号，与英文侧的 Discord 并行。

## F. 盲区分析（源码复杂但社区忽视，值得本研究重点补）

有些地方源码里很讲究，社区却几乎没聊到——这些正是本研究打算重点补上的：

- **能力接缝的三个角色**是怎么做到"换一个 provider 就能改变整个产品"的——所谓能力接缝，可以类比成插座标准：只要接口一致，换个灯泡不用重新布线。这块社区几乎没展开，放到 Ch11–14 重点讲。
- **事件溯源里"model-visible⟺logged"这条不变量**（凡是模型能看到的，必然都被记进日志，反之亦然）在工程上意味着什么——Ch07 重点。
- **面向 agent 的工程纪律**（Agent Notes 的生命周期、各种门禁体系）——这部分被"代码是 agent 写的"这个猎奇点盖住了，Ch20 系统补上。
- **能自我修改的运行时**（extensions/demo:cordis）和前面讲的时空可组合性是什么关系——Ch18 结合 Ch03 一起看。

## G. 引用纪律

最后交代一下数字怎么用。外部数字（star 数、下载体积、HN 分数）会随时间变，而且大多是二手的，所以正文里引用它们时一律标 `[claimed]` 或注明抓取日期；一旦和源码对不上，以一手源码为准。举个真实的例子：曾有一次 WebFetch 对着 GitHub 页面"幻觉"出了 "61.3k stars/12,293 commits" 这样的数字，已经弃用——其中 commits 数后来由本地 `git log` 独立核实，确实是 12,293。
